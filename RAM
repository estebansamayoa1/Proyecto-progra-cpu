class RAM:
    def __init__(self, instructions, upcode):
        self.instructions=open('machinecode.cpufm', 'r')
        self.upcode=[]
        for line in self.instructions:
            if not line.lower().startswith(";"):
                self.upcode.append(line)

